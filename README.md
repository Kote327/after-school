#プロダクドゴール
- このアプリを通じてユーザーはグループで予定を確認できるようになる。

## 使い方

`index.html` をブラウザで開くと、グループの予定を確認できます。

- リスト表示と週表示を切り替え
- キーワード・カテゴリーで予定を絞り込み
- 「予定を追加」から予定を登録

追加した予定はブラウザの `localStorage` に保存されます。ローカルサーバーを使う場合は、プロジェクトフォルダーで `python3 -m http.server 4173` を実行してください。